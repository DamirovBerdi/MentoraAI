### Gemini API keys example
# Copy this file to a secure location (do NOT commit real keys to git).
# Then replace the placeholder values with your real keys.
# You can set the environment variable GEMINI_API_KEYS to the comma-separated list of keys
# or set GEMINI_API_KEY_1, GEMINI_API_KEY_2, ... individually.

# Example (do NOT keep literal 'KEY1' values):
# GEMINI_API_KEYS=KEY1,KEY2,KEY3,KEY4,KEY5,KEY6,KEY7,KEY8,KEY9

# Save this file as .env (or similar) and load it into your shell before starting the server.
# For PowerShell you can run:
#   Get-Content .\gemini_keys_example.env | ForEach-Object { if ($_ -match '^\s*([^#][^=]*)=(.*)$') { $name=$matches[1].Trim(); $val=$matches[2].Trim(); Set-Item -Path Env:$name -Value $val } }

# Or set a single env var for the current session (replace the placeholders with your keys):
#   $env:GEMINI_API_KEYS = 'KEY1,KEY2,KEY3'

# To make the variable persistent for your Windows user (replace placeholders):
#   setx GEMINI_API_KEYS "KEY1,KEY2,KEY3"

# Security reminder: do not commit real keys into the repository or share them publicly.
